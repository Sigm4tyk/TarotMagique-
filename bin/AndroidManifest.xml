<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.tarotmagique"
    android:versionCode="1"
    android:versionName="1.0" >

    <uses-sdk
        android:minSdkVersion="19"
        android:targetSdkVersion="21" />

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.CHANGE_WIFI_STATE" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/AppTheme" >
        <activity
            android:name=".MainActivity"
            android:label="@string/app_name" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".TableJoueurActivity"
            android:label="@string/labelActivityTableJoueur" >
        </activity>
        <activity
            android:name=".ListeAtoutsActivity"
            android:label="@string/labelActivityRegarderAtouts" >
        </activity>
        <activity
            android:name=".ParametresConnexionPartieActivity"
            android:label="@string/labelActivityParametresConnexionPartie" >
        </activity>
        <activity
            android:name=".ParametresHebergerActivity"
            android:label="@string/labelActivityParametresHeberger" >
        </activity>
        <activity
            android:name=".HistoriqueActivity"
            android:label="@string/labelActivityHistorique" >
        </activity>
    </application>

</manifest>